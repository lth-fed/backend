{
  "db_name": "PostgreSQL",
  "query": "with ensure_membership as (\n            insert into group_memberships (user_id, group_id)\n            values ($1, $2)\n            on conflict do nothing\n        ), upsert_adminship as (\n            insert into group_adminships (user_id, group_id)\n            values ($1, $2)\n            -- Noop because if we had `on conflict do nothing`, nothing would be\n            -- returned on conflict.\n            on conflict(user_id, group_id) do update set user_id = $1, group_id = $2\n            returning user_id, group_id\n        )\n        select ua.user_id, g.path as \"group_path!: Path\"\n        from upsert_adminship ua\n        join groups g on g.id = ua.group_id",
  "describe": {
    "columns": [
      {
        "ordinal": 0,
        "name": "user_id",
        "type_info": "Text"
      },
      {
        "ordinal": 1,
        "name": "group_path!: Path",
        "type_info": {
          "Custom": {
            "name": "ltree",
            "kind": "Simple"
          }
        }
      }
    ],
    "parameters": {
      "Left": [
        "Text",
        "Uuid"
      ]
    },
    "nullable": [
      false,
      false
    ]
  },
  "hash": "463dbfcdb1379b01fa6fe888bcf9b10ddf30a699e8c3696088fbd947c2d47352"
}
